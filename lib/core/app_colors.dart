import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color primary = Color(0xFFF97316);   // Orange
  static const Color secondary = Color(0xFF2563EB); // Blue

  // App surfaces
  static const Color bg = Color(0xFFF7F7FB);        // Scaffold background
  static const Color surface = Color(0xFFFFFFFF);  // <--- FIXED (NEW)
  static const Color card = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFE5E7EB);

  // Text
  static const Color textDark = Color(0xFF0F172A);
  static const Color textLight = Color(0xFF64748B);
}
